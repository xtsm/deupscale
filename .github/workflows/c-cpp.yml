name: C/C++ CI

on:
  push:
    branches: [ "dungeon-master" ]
  pull_request:
    branches: [ "dungeon-master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: make test
      run: make test
